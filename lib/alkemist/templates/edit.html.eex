<div class="label text-uppercase mt-3 mb-2">Edit <%= @singular_name %></div>

<div class="row mt-3">
  <% {view, partial} = @form_partial %>
  <%= render(view, partial, Map.put(assigns, :action, action_path(@struct, [@conn, :update, @resource]))) %>
</div>
