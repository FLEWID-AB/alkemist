defmodule Alkemist do
  @moduledoc """
  Alkemist builds admin interfaces for Phoenix applications.

  Start with `Alkemist.Controller` (the CRUD macros), `Alkemist.Router`
  (`alkemist_resources/2`), `Alkemist.Config` (the `config :my_app, Alkemist` keys) and
  `Alkemist.Theme` (how to change the look). Assets are served from `priv/static/assets`;
  see the README for the `Plug.Static` line.
  """
end
